{
  "version": 2,
  "buildCommand": "npm install && npm run build --workspace=packages/config && npm run build --workspace=packages/types && npm run build --workspace=packages/validation && npm run build --workspace=apps/api",
  "outputDirectory": "apps/api/dist",
  "installCommand": "npm install",
  "functions": {
    "apps/api/src/index.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "routes": [
    {
      "source": "/(.*)",
      "destination": "apps/api/src/index.ts"
    }
  ]
}